import InfoSection from './InfoSection';

const Component = {
  title: 'InfoSection',
  component: InfoSection,
};

const Template = () => <InfoSection />;

export const Default = Template.bind({});
export default Component;
