import React from 'react';
import InfoSubs from './InfoSubs';

const Component = {
  title: 'InfoSubs',
  component: InfoSubs,
};

const Template = () => <InfoSubs />;

export const Default = Template.bind({});
export default Component;
